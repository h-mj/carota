import { DefaultSceneProps, Scene } from "./Scene";

export class Administration extends Scene<"Administration"> {
  public constructor(props: DefaultSceneProps<"Administration">) {
    super("Administration", props);
  }

  public render() {
    return null;
  }
}
