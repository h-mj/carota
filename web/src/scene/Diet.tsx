import { DefaultSceneProps, Scene } from "./Scene";

export class Diet extends Scene<"Diet"> {
  public constructor(props: DefaultSceneProps<"Diet">) {
    super("Diet", props);
  }

  public render() {
    return null;
  }
}
