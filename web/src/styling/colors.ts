/**
 * Background color.
 */
export const BACKGROUND = "rgb(255, 255, 255)";

/**
 * Background color with small transparency.
 */
export const BACKGROUND_TRANSLUCENT = "rgba(255, 255, 255, .9)";

/**
 * Foreground (text) color.
 */
export const FOREGROUND = "rgb(0, 0, 0)";

/**
 * Border color around components when  component is in default state.
 */
export const DEFAULT_BORDER = "rgba(0, 0, 0, .25)";

/**
 * Label color of component when component is in default state.
 */
export const DEFAULT_LABEL = "rgba(0, 0, 0, .5)";

/**
 * Color of borders and labels when component is in active state.
 */
export const ACTIVE = "rgba(0, 0, 0, 1)";

/**
 * Color of borders and labels when component is in error state.
 */
export const ERROR = "rgba(220, 0, 0)";
